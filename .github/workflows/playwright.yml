name: Playwright BDD Tests
permissions:
  contents: write      
  pull-requests: write 
  issues: write        
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright BDD tests
      id: tests
      run: npm run test:ci  
      continue-on-error: true
    - name: Heal Tests with Agent
      if: steps.tests.outcome == 'failure'
      run: |
        gh agent-task create "The Playwright tests failed. Analyze the result cucumber-report.html in reports folder in the last run and fix the issue."
      env:
        GH_TOKEN: ${{ secrets.AGENT_PAT }}
    - name: Test Results Summary
      if: always()
      run: |
        if [ "${{ steps.tests.outcome }}" == "failure" ]; then
          echo "⚠️ Some UI tests failed. Check the HTML report for details."
        else
          echo "✅ All UI tests passed!"
        fi
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: bdd-report
        path: reports/cucumber-report.html
        retention-days: 30
